server:
  port: 8081
spring:
  application:
    name: payment
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_gateway
    username: postgres
    password: 1234
    driver-class-name: org.postgresql.Driver
  data:
    redia:
      port: 6379
      host: localhost

  jpa:
    hibernate:
      ddl-auto: update